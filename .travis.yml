language: ruby

RVM:
  - 1.9.3
  - 2.2.3

git:
  submodules: false
  
sudo: false

install:
  - gem update --system
  - gem --version

script:
  - ruby --verbose download-atlas-upload.rb

after_script:
  - ls -al
  - pwd

env:
  - LANG="en_US.UTF-8"

os:
  - linux
